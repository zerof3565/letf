# AGENTS.md

## Virtual Environment

Always activate the virtual environment before installing any pip packages:

```bash
source .venv/bin/activate
pip install <package>
```

Do not install packages globally or outside of `.venv`.
